{% extends "dashboard.html.twig" %}

{% block body %}
<div class="col-lg-6">
    <h1>Creación de Falta</h1>

    {{ form_start(form) }}
    <div style="display: none;">{{ form_row(form.docente) }}</div>
        {{ form_widget(form) }}
        
        <input class="btn btn-primary" type="submit" value="Crear" />
    {{ form_end(form) }}

    <a href="{{ path('docentes_falta_index') }}">Volver</a>
</div>
{% endblock %}
{% block javascripts %}
    <script>
        var usuario = "{{ app.user.id }}";
        console.log(usuario);
        $("#falta_docente").val(usuario);
    </script>
{% endblock %}   